# JurídicoBRM

Leia e siga **AGENTS.md** (instruções completas de arquitetura, padrão visual e
regras de dados) e **docs/FUNCIONALIDADES.md** (contrato de funcionalidades —
nada listado lá pode ser removido em refatorações) antes de alterar código.

Checks obrigatórios ao final: `npm run typecheck`, `npm run lint`, `npm run build`.
